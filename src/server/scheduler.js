//import cron from 'node-cron';
import queryPackageTransactionBlock from './quertPackageTransaction';

// 启动定时任务
export function startScheduler() {
  // 每5分钟执行一次查询
  //cron.schedule('*/5 * * * *', async () => {
  //  console.log('开始执行交易查询任务...');
  //  try {
  //    const senders = await queryPackageTransactionBlock();
  //    console.log('查询完成，符合条件的发送者数量:', senders.length);
      
  //  } catch (error) {
  //    console.error('定时任务执行失败:', error);
  //  }
  //});
  //*/
}